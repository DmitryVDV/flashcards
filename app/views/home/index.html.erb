<div class="container">
  <p style="font-size: 22px;">
    Первый в мире удобный менеджер флеш-карточек. Именно так.
  </p>
  <div class="row">

    <% if @card == nil %>
      <div class="container">
        <p>Больше нет карточек подходящих по условию 3 дня!</p>
      </div>
    <% else %>
      <div class="col-md-4">
        <%= image_tag(@card.image.url) if @card.image? %>
      </div>
      <div class="col-md-4">
        <p class="text-center">
          <%= link_to @card.original_text, card_path(@card), id: 'link_to_card' %>
        </p>
      </div>
      <div class="col-md-4">
        <%= form_tag checktranslation_path do %>
          <%= text_field_tag :translation %>
          <%= hidden_field_tag :card_id, @card.id %>
          <button>Проверить</button>
        <% end %>
      </div>
    <% end %>
  </div>
  <div>
  <% flash.each do |name, msg| -%>
      <%= content_tag :div, msg, class: name %>
    <% end -%>
  </div>
</div>

